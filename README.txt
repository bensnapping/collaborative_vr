The UnityProject folder is where the Unity files are kept for the main scene.
The AvatarAssets folder is where we can store avatar files as Travis makes changes to them.

In order to make changes to the 